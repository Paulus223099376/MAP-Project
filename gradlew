#!/bin/sh
# Placeholder. Open this project in Android Studio (it will create
# gradle/wrapper/gradle-wrapper.jar automatically) or run
# `gradle wrapper --gradle-version 8.10.2` in this directory once,
# then use the generated `./gradlew`.
echo "Open this project in Android Studio, or run 'gradle wrapper' once to generate the wrapper jar." 1>&2
exit 1
